name: 🧪 Test Workflow

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Test Step
        run: |
          echo "🧪 Test workflow is working!"
          echo "🕐 Current time: $(date)"
          echo "🎯 If you see this, workflows are enabled"
